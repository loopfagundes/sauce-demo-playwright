pre-commit:
  commands:
    prettier:
      run: npx prettier --write {staged_files}
      glob: '*.{js,mjs,json,md}'
      stage_fixed: true

    eslint:
      run: npx eslint {staged_files}
      glob: "*.{js,mjs,cjs}"
      stage_fixed: true